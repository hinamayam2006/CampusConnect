PORT=5000
MONGODB_URI=
JWT_SECRET=
REFRESH_TOKEN_SECRET=
CLIENT_URL=http://localhost:3000
# Used by POST /api/upload/image (listing photos). Optional until you test uploads.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Optional: run `npm run seed` from the backend folder to load demo users (password Test@1234) + listings + rides
EMAIL_USER=
EMAIL_APP_PASSWORD=